import { describe, it } from "vitest";

describe("Transaction core entity tests", () => {
    it("newTransaction function without parameters", () => {})
    it("newTransaction function with null parameters", () => {})
    it("newTransaction function with undefined parameters", () => {})

    it("newTransaction function with chainId parameter undefined", () => {})
    it("newTransaction function with chainId parameter value isn't a string", () => {})
    it("newTransaction function with chainId parameter value is a string", () => {})

    it("newTransaction function with rpc parameter undefined", () => {})
    it("newTransaction function with rpc parameter value isn't a string", () => {})
    it("newTransaction function with rpc parameter value is a string", () => {})

    it("newTransaction function with gasLimit parameter undefined", () => {})
    it("newTransaction function with gasLimit parameter value isn't a string", () => {})
    it("newTransaction function with gasLimit parameter value is a string", () => {})

    it("newTransaction function with fees parameter undefined", () => {})
    it("newTransaction function with fees parameter value isn't a string", () => {})
    it("newTransaction function with fees parameter value is a string", () => {})

    it("newTransaction function with memo parameter undefined", () => {})
    it("newTransaction function with memo parameter value isn't a string", () => {})
    it("newTransaction function with memo parameter value is a string", () => {})

    it("newTransaction function with messages parameter undefined", () => {})
    it("newTransaction function with messages parameter value isn't an array", () => {})
    it("newTransaction function with messages parameter value is an array", () => {})

    it("newTransaction function with valid parameters", () => {})
})