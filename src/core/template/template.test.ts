import { describe, it } from "vitest";

describe("Template core entity tests", () => {
    it("newTemplate function without parameters", () => {})
    it("newTemplate function with null parameters", () => {})
    it("newTemplate function with undefined parameters", () => {})

    it("newTemplate function with name parameter undefined", () => {})
    it("newTemplate function with name parameter value isn't a string", () => {})
    it("newTemplate function with name parameter value is a string", () => {})

    it("newTemplate function with gasLimit parameter undefined", () => {})
    it("newTemplate function with gasLimit parameter value isn't a string", () => {})
    it("newTemplate function with gasLimit parameter value is a string", () => {})

    it("newTemplate function with fees parameter undefined", () => {})
    it("newTemplate function with fees parameter value isn't a string", () => {})
    it("newTemplate function with fees parameter value is a string", () => {})

    it("newTemplate function with messages parameter undefined", () => {})
    it("newTemplate function with messages parameter value isn't an array", () => {})
    it("newTemplate function with messages parameter value is an array", () => {})

    it("newTemplate function with isMultisig parameter undefined", () => {})
    it("newTemplate function with isMultisig parameter value is a boolean", () => {})
    it("newTemplate function with isMultisig parameter value isn't a boolean", () => {})

    it("newTemplate function with valid parameters", () => {})
})